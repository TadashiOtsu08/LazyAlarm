import Foundation

/// SF Symbolsのアイコン名定数
/// アイコン名を直接文字列で書かずここから呼び出す
enum SFSymbols {
    /// アラームアイコン（ホームタブ）
    static let alarm = "alarm.fill"
    /// ベルアイコン（アラートタブ）
    static let bell = "bell.fill"
    /// キャラクターアイコン（キャラクタータブ）
    static let character = "pawprint.fill"
    /// プラスアイコン（アラーム追加ボタン）
    static let plus = "plus"
}
